language: node_js
node_js:
  - 14
cache: npm

before_script:
  - npm install -g @nestjs/cli
  - cd 09-app-rmq
  - npm install

script:
  - npm run lint
  
after_success:
  - nest build auth
  - nest build user
  - nest build tickets

branches:
  only:
    - main
